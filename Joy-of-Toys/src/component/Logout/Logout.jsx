// eslint-disable-next-line no-unused-vars
import React from 'react';

const Logout = () => {
    return (
        <div>
            this is logout page.
        </div>
    );
};

export default Logout;